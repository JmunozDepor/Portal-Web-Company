namespace PortalSaas.Host.Resources;

/// <summary>
/// Clase dummy sin miembros -- solo un ancla de tipo para IStringLocalizer&lt;SharedResources&gt;/
/// IHtmlLocalizer&lt;SharedResources&gt;. El contenido real vive en SharedResources.es.resx/
/// SharedResources.en.resx (mismo namespace/carpeta, resuelto por convención de
/// nombre + ResourcesPath = "Resources" configurado en AddLocalization, ver Program.cs).
/// Alcance de esta entrega: solo las llaves de navegación/acciones base -- ver el
/// comentario de Program.cs sobre qué NO cubre todavía (contenido de los plugins).
/// </summary>
public sealed class SharedResources
{
}
