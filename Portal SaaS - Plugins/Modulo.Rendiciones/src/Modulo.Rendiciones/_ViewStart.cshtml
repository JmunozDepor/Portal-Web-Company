@{
    // 2026-08-17: migrado de "_Layout.cshtml" a "_ModuloLayout.cshtml" (rail de
    // 64px + sub-nav contextual + design-tokens.css/components.css globales) --
    // mismo cambio ya aplicado a los plugins internos del Core. Requiere que
    // _ModuloLayout.cshtml/_LayoutMaestro.cshtml existan en el Host (ver ese
    // repo, plan de gobernanza UI 2026-08-17).
    Layout = "/Pages/Shared/_ModuloLayout.cshtml";
}
